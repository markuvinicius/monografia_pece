extends base-layout

block content    

  section  
 
    div(class="container")
      if (typeof tweets != "undefined")    
        if tweets.length
          form(action='save' method="post" id="formTweets") 
            input(type="hidden", value=user, name="usuario_update")                    
            each tweet in tweets
              div(class="card")
                div(class="card-body")
                  h5(class="card-title") 
                    p=tweet.usuario                                 
                  p(class="card-text")
                    p=tweet.texto
                  
                  div(class="row")
                    div(class="col")
                      input(type="radio", name=tweet.id, value="Sim", id="label") 
                      label(class="form-check-label" for=tweet.id) Acidente                      
                    div(class="col")
                      input(type="radio", name=tweet.id, value="Não", id="label") 
                      label(class="form-check-label" for=tweet.id) Não Acidente
            div(class="container")
              <br/>
              input(type="submit", 
                    value="Salvar",
                    class="btn btn-lg btn-primary btn-block") 

        div(class="container") 
          <br/>
          <br/>
          <br/>

      else
        p Sem dados para exibir :(
